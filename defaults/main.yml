---
kafka_version: 3.6.0
kafka_scala_version: 2.13
kafka_archive:
  url: "https://dlcdn.apache.org/kafka/{{ kafka_version }}/kafka_{{ kafka_scala_version }}-{{ kafka_version }}.tgz"
  dest: /opt/kafka

kafka_user: kafka
kafka_group: kafka
kafka_datadir: /var/kafka

kafka_zookeeper_datadir: /var/zookeeper
